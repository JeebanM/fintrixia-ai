import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactCompiler disabled to prevent JavaScript heap out of memory errors
  // reactCompiler: true,
};

export default nextConfig;
